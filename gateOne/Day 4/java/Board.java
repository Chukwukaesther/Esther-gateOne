import java.util.Scanner;
public class Board {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);

		String[][] alphabet = new String[3][3];
		
		int rows = alphabet.length;
		int column = alphabet[0].length;

		System.out.println("Rows: " + rows);
		System.out.println("Column: " + column);
		

		for(int digit = 0; digit < 9; digit++) {
			System.out.print("Enter alpahbet: ");
			String alphabet = scanner.nextLine();
		}
	

		for (int count = 0; count < alphabet.length; count++){
			for (int counter = 0; counter < alphabet.length; counter++){
				alphabetTime[count][counter] = diit;
			}
		}
	}
}